[Unit]
Description=Kanata 75-key (default)
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/kanata -c %h/.config/kanata/kanata-vim-75.kbd --no-wait
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
